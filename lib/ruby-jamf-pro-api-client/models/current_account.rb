=begin
#Jamf Pro API

### Overview The Jamf Pro API is a RESTful API for Jamf Pro built to enable consistent and efficient programmatic access to Jamf Pro.<br/><br/> The swagger schema can be found [here](/api/schema/). 

The version of the OpenAPI document: production

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 6.1.0-SNAPSHOT

=end

require 'date'
require 'time'

module JamfPro
  class CurrentAccount
    attr_accessor :id

    attr_accessor :username

    attr_accessor :real_name

    attr_accessor :email

    attr_accessor :preferences

    attr_accessor :is_multi_site_admin

    attr_accessor :access_level

    attr_accessor :privilege_set

    attr_accessor :privileges

    attr_accessor :group_ids

    attr_accessor :current_site_id

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'id' => :'id',
        :'username' => :'username',
        :'real_name' => :'realName',
        :'email' => :'email',
        :'preferences' => :'preferences',
        :'is_multi_site_admin' => :'isMultiSiteAdmin',
        :'access_level' => :'accessLevel',
        :'privilege_set' => :'privilegeSet',
        :'privileges' => :'privileges',
        :'group_ids' => :'groupIds',
        :'current_site_id' => :'currentSiteId'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'id' => :'Integer',
        :'username' => :'String',
        :'real_name' => :'String',
        :'email' => :'String',
        :'preferences' => :'AccountPreferences',
        :'is_multi_site_admin' => :'Boolean',
        :'access_level' => :'String',
        :'privilege_set' => :'String',
        :'privileges' => :'Array<String>',
        :'group_ids' => :'Array<Integer>',
        :'current_site_id' => :'Integer'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `JamfPro::CurrentAccount` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `JamfPro::CurrentAccount`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'id')
        self.id = attributes[:'id']
      end

      if attributes.key?(:'username')
        self.username = attributes[:'username']
      end

      if attributes.key?(:'real_name')
        self.real_name = attributes[:'real_name']
      end

      if attributes.key?(:'email')
        self.email = attributes[:'email']
      end

      if attributes.key?(:'preferences')
        self.preferences = attributes[:'preferences']
      end

      if attributes.key?(:'is_multi_site_admin')
        self.is_multi_site_admin = attributes[:'is_multi_site_admin']
      end

      if attributes.key?(:'access_level')
        self.access_level = attributes[:'access_level']
      end

      if attributes.key?(:'privilege_set')
        self.privilege_set = attributes[:'privilege_set']
      end

      if attributes.key?(:'privileges')
        if (value = attributes[:'privileges']).is_a?(Array)
          self.privileges = value
        end
      end

      if attributes.key?(:'group_ids')
        if (value = attributes[:'group_ids']).is_a?(Array)
          self.group_ids = value
        end
      end

      if attributes.key?(:'current_site_id')
        self.current_site_id = attributes[:'current_site_id']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      access_level_validator = EnumAttributeValidator.new('String', ["FullAccess", "SiteAccess", "GroupBasedAccess"])
      return false unless access_level_validator.valid?(@access_level)
      privilege_set_validator = EnumAttributeValidator.new('String', ["ADMINISTRATOR", "AUDITOR", "ENROLLMENT", "CUSTOM"])
      return false unless privilege_set_validator.valid?(@privilege_set)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] access_level Object to be assigned
    def access_level=(access_level)
      validator = EnumAttributeValidator.new('String', ["FullAccess", "SiteAccess", "GroupBasedAccess"])
      unless validator.valid?(access_level)
        fail ArgumentError, "invalid value for \"access_level\", must be one of #{validator.allowable_values}."
      end
      @access_level = access_level
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] privilege_set Object to be assigned
    def privilege_set=(privilege_set)
      validator = EnumAttributeValidator.new('String', ["ADMINISTRATOR", "AUDITOR", "ENROLLMENT", "CUSTOM"])
      unless validator.valid?(privilege_set)
        fail ArgumentError, "invalid value for \"privilege_set\", must be one of #{validator.allowable_values}."
      end
      @privilege_set = privilege_set
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          id == o.id &&
          username == o.username &&
          real_name == o.real_name &&
          email == o.email &&
          preferences == o.preferences &&
          is_multi_site_admin == o.is_multi_site_admin &&
          access_level == o.access_level &&
          privilege_set == o.privilege_set &&
          privileges == o.privileges &&
          group_ids == o.group_ids &&
          current_site_id == o.current_site_id
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, username, real_name, email, preferences, is_multi_site_admin, access_level, privilege_set, privileges, group_ids, current_site_id].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = JamfPro.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
